<UserControl x:Class="Fotopodglad.Controls.ZoomableImage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             ClipToBounds="True"
             Background="Transparent">
    <Grid x:Name="RootGrid">
        <Grid x:Name="ImageLayer">
            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1"/>
                    <TranslateTransform x:Name="TranslateTransform" X="0" Y="0"/>
                </TransformGroup>
            </Grid.RenderTransform>
            <Grid.RenderTransformOrigin>
                <Point X="0.5" Y="0.5"/>
            </Grid.RenderTransformOrigin>
            <Image x:Name="PhotoImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"
                   SnapsToDevicePixels="True" UseLayoutRounding="True"/>
            <Image x:Name="OverlayImage" Stretch="Uniform" IsHitTestVisible="False"/>
        </Grid>
    </Grid>
</UserControl>
